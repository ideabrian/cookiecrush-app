{
  "name": "claude-hooks-observatory",
  "version": "1.0.0",
  "description": "Neo-brutalist dashboard for Claude Code hook execution monitoring",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute hooks-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute hooks-db --remote --file=./schema.sql"
  },
  "keywords": ["claude-code", "hooks", "monitoring", "cloudflare"],
  "author": "GTME HQ",
  "license": "MIT"
}
